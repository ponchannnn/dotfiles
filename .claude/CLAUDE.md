## Git

- Before staging anything, always check `git status` / `git diff` first.
- Use `git rm` instead of plain `rm` to remove tracked files.
- Commit messages: English, Conventional Commits style.
- When fixing a comment, rewrite it in full rather than leaving "previously did X" style history notes.
